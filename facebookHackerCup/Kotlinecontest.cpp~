#include<iostream>
#include<algorithm>
#include<vector>
#include<map>
using namespace std;
int main(){

	  int size;
	  int T;
	  vector<int>vs;

	  vector<int>vs2;

	  cin>>T;

	  while(T--){

		   int m;
           int count1=0;
		   int count2=0;
         
		   cin>>size;

		   for(int i=0;i<size;i++){

			    cin>>m;
				vs.push_back(m);
		   }

		   int num=vs[0];
		   int num2=vs[vs.size()/2];
		   int num3=vs[1];
		 
      }
}
